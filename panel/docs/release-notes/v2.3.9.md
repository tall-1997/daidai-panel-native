# v2.3.9
<!-- release-title: 通知换行、窗口最小化、Python 版本回退与随机延迟修复 -->

## 版本概览

`v2.3.9` 是 `v2.3.8` 之后的维护版本。本轮集中修复社区反馈的 4 个问题（issue #67~#70）：企业微信应用图文消息正文换行、窗口最小化被强制全屏、桌面版 Python 版本缺失、随机延迟误伤手动执行，以及多设备登录后旧会话无限加载。

## 修复

- **修复企业微信应用 mpnews 图文消息正文不换行**：mpnews 的正文（content）按 HTML 渲染，此前正文里的换行不生效。现在正文中的换行会正确转换为 `<br>` 显示；描述（digest）等纯文本字段以及 text、markdown 等其它消息类型的换行行为保持不变，不受影响。
- **修复 Edge/Chromium 下点击最小化被强制拉回全屏**：窗口最小化时浏览器会上报窗口宽度为 0，此前被误判为移动端，导致弹窗全屏、侧栏收起、页面切到移动端布局（切到别的标签页再切回来才恢复）。现在对“宽度为 0/页面隐藏”的异常尺寸加了守卫，最小化不再翻转布局；真实移动端与正常窗口缩放不受影响。
- **修复桌面版 Python 版本缺失直接启动失败**：当任务配置的 Python 版本（如 3.12）本机未安装、但装了其它受支持版本（如 3.11）时，面板会自动回退到已安装的版本运行，并在日志中提示“已回退到 x.x”，不再直接报“Python x.x 不可用”而中断。Windows 上仅通过 `py` 启动器安装的 Python 也能被正确识别。任务表单会标注未安装的版本，新任务默认选中已安装的版本。桌面/二进制版与面具（Magisk）版回退行为一致；已安装所请求版本时仍然尊重该版本、不会回退。
- **修复随机延迟误延迟手动/开机任务**：设置“随机延迟最大秒数”后，此前手动点击执行和开机运行的任务也会被排队延迟。现在随机延迟只对定时（cron）触发的任务生效，用于错峰；手动执行和开机运行的任务立即运行，不再等待。
- **修复多设备登录后旧会话无限加载**：当同时在线设备超过“同时在线人数”限制、较早的登录被顶下线后，旧设备/页面此前会卡在无限加载，必须退出重新登录。现在旧会话的请求会正确失效并跳转到登录页，不再卡死。

## 升级说明

- Python 版本回退是自动的，无需额外配置。如果你希望某个任务必须使用特定版本，请先在设备上安装对应版本，或在任务设置里切换 Python 版本。
- 手动执行任务不再等待随机延迟属于预期变化；如需对某任务错峰，请通过定时（cron）触发。
- 以上修复不涉及数据结构变更，升级后无需迁移，历史任务与配置保持兼容。

## 验证

- 后端回归测试：覆盖企业微信 mpnews 正文换行转 `<br>` 且描述（digest）保留原始换行、随机延迟按触发来源（cron 延迟 / manual、startup 跳过）区分、Python 版本回退（已安装不回退 / 缺失回退到已装版本 / 全都没装保持请求版本 / 单版本运行时不回退）。
- 前端构建与类型检查（`npm run build` + vue-tsc）通过：窗口最小化守卫、登录刷新队列失败逐个 reject、任务表单 Python 版本可用性透传。
- 最终发布前执行：`go test ./...`、`npm run build` 与 `scripts/release-preflight.ps1 -Version 2.3.9`。
