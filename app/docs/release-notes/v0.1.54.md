# v0.1.54 Analyzer 与生命周期修订

- 启动页、登录页、订阅弹层、通知、SSH 密钥和用户管理补齐异步生命周期检查。
- 14 处 DropdownButtonFormField 从弃用的 value 迁移到 initialValue。
- 删除用户卡冗余 WidgetRef 和父级 BuildContext 字段。
- 清理图片错误回调多余下划线参数。
- 保留 ReorderableListView 当前索引行为，避免无设备回归验证时引入拖拽顺序变化。
