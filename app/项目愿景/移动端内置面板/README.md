# 移动端内置面板项目愿景

## 愿景

用户只安装呆呆面板 App，即可在手机本机创建、管理和运行面板，无需另行部署 Docker、服务器或独立后端面板。

目标产品同时保留两种连接方式：

- 本地模式：App 管理并访问手机内置面板实例。
- 远程模式：App 连接用户已有的远程呆呆面板实例。

## 分析结论

该愿景可分平台、分能力等级实现。

| 产品形态 | 可行性 | 目标能力 |
|---|---:|---|
| Android 普通设备 | 中高 | 内置 API、SQLite、任务管理、有限后台调度、受控脚本运行时 |
| Android Root 设备 | 高 | 复用 Magisk/Linux 运行环境，接近后端面板完整能力 |
| iOS 普通设备 | 中 | 内置数据与管理核心、前台自动化、系统允许的后台任务 |
| iOS 完整脚本执行 | 低 | 受 App Sandbox、后台时长、动态代码执行和分发规则限制 |

“全功能”需要按平台定义：

- 业务管理全功能：任务、脚本、日志、环境变量、订阅、通知、用户、安全、备份、Open API 和系统设置。
- 本地执行全功能：Python、Node.js、Shell、TypeScript、Go、Git 订阅、依赖安装和长期后台调度。
- Android Root 版可将两类能力都作为目标。
- Android 普通版可覆盖业务管理全功能，并以受控运行时覆盖主要本地执行能力。
- iOS 版可覆盖业务管理全功能的本地数据形态，本地执行能力按系统许可提供；完整执行由远程实例承载。

## 推荐路线

采用“共享领域核心 + 平台运行宿主 + 现有 Flutter UI”的架构：

1. 将后端核心能力拆为可嵌入的共享服务层。
2. Android 先实现本地 Sidecar 后端与受控运行时。
3. Root Android 复用上游 Magisk 容器能力形成完整版本。
4. iOS 实现沙箱内本地核心，并将完整脚本执行路由到远程执行节点。
5. App 自动发现本地实例，将 `127.0.0.1` 本地连接作为一种受管服务器配置。

## 文档索引

- `01-可行性分析.md`：平台约束、可实现范围和关键结论。
- `02-产品需求.md`：EARS 格式需求与验收标准。
- `03-目标架构.md`：共享核心、平台宿主和 App 集成架构。
- `04-功能兼容矩阵.md`：后端全部功能在 Android、Root Android、iOS 的实现等级。
- `05-运行时与脚本方案.md`：Python、Node.js、Shell、TypeScript、Go 和 Git 的实现方案。
- `06-数据安全与更新.md`：数据目录、密钥、备份、升级和回滚设计。
- `07-实施路线图.md`：验证原型、MVP、平台扩展和发布门禁。
- `08-待确认决策.md`：进入开发前需要确定的产品与分发决策。

## 参考基线

- 当前 App：Flutter、Riverpod、GoRouter、Dio，已覆盖后端主要管理 API。
- 上游面板：Go、Gin、SQLite，Go 二进制可同时提供 API 与 Web 静态资源。
- 上游 Android Root 方案：Magisk 模块通过 Linux 容器提供 Python、Node.js、Git 和后端服务。
- 上游版本基线：`daidai-panel v2.3.9`。
