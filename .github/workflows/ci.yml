name: PR and Main Quality

on:
  pull_request:
    branches:
      - main
  push:
    branches:
      - main

permissions:
  contents: read
  pull-requests: read

concurrency:
  group: quality-${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

env:
  GO_VERSION: 1.25.0
  FLUTTER_VERSION: 3.41.5
  JAVA_VERSION: '17'
  GRADLE_VERSION: 9.1.0
  NODE_VERSION: '20.19.x'
  NPM_VERSION: '10.8.2'

jobs:
  changes:
    name: Detect changed areas
    runs-on: ubuntu-latest
    timeout-minutes: 5
    outputs:
      go: ${{ steps.filter.outputs.go }}
      route: ${{ steps.filter.outputs.route }}
      flutter: ${{ steps.filter.outputs.flutter }}
      kotlin: ${{ steps.filter.outputs.kotlin }}
      panel_web: ${{ steps.filter.outputs.panel_web }}
      release_scripts: ${{ steps.filter.outputs.release_scripts }}
    steps:
      - name: Checkout source
        uses: actions/checkout@v5

      - name: Classify changed paths
        id: filter
        uses: dorny/paths-filter@v3
        with:
          filters: |
            go:
              - '.github/workflows/ci.yml'
              - 'panel/server/**'
              - 'go.work'
              - 'go.work.sum'
            route:
              - '.github/workflows/ci.yml'
              - 'panel/server/router/**'
              - 'panel/server/handler/**'
              - 'scripts/generate-route-contract.go'
              - 'contracts/backend-api-mobile.json'
              - 'go.work'
              - 'go.work.sum'
            flutter:
              - '.github/workflows/ci.yml'
              - 'app/lib/**'
              - 'app/test/**'
              - 'app/pubspec.yaml'
              - 'app/pubspec.lock'
              - 'app/analysis_options.yaml'
            kotlin:
              - '.github/workflows/ci.yml'
              - 'app/android/**'
              - 'app/pubspec.yaml'
              - 'app/pubspec.lock'
              - 'app/scripts/prepare-android-python-runtime.sh'
              - 'app/scripts/prepare-android-yaegi-runtime.sh'
              - 'app/scripts/prepare-android-ubuntu-rootfs.sh'
              - 'app/scripts/prepare-android-native-source-build.sh'
              - 'app/android/app/src/main/jni/**'
              - 'app/scripts/verify-android-linux-runtime.py'
              - 'app/scripts/test_verify_android_linux_runtime.py'
              - 'app/scripts/test_prepare_android_ubuntu_rootfs.py'
              - 'app/scripts/rootfs-trusted-sources.json'
              - 'app/runtime-tools/yaegi/**'
              - 'panel/web/**'
              - 'panel/server/go.mod'
              - 'panel/server/go.sum'
              - 'runtime/manifest.json'
              - 'runtime/compatibility.json'
              - 'runtime/smoke-evidence.json'
            panel_web:
              - '.github/workflows/ci.yml'
              - 'panel/web/**'
            release_scripts:
              - '.github/workflows/ci.yml'
              - '.github/workflows/android-release.yml'
              - '.github/workflows/android-device-smoke.yml'
              - 'VERSION.json'
              - 'app/pubspec.yaml'
              - 'panel/web/package.json'
              - 'panel/web/package-lock.json'
              - 'runtime/**'
              - 'scripts/run-runtime-smoke.go'
              - 'scripts/run-runtime-smoke_test.go'
              - 'scripts/verify-runtime-contract.go'
              - 'scripts/verify-runtime-contract_test.go'
              - 'scripts/verify-runtime-elf.go'
              - 'scripts/verify-runtime-elf_test.go'
              - 'scripts/version.py'
              - 'scripts/test_version.py'
              - 'scripts/android-runtime-smoke.py'
              - 'scripts/test_android_runtime_smoke.py'
              - 'scripts/generate-release-evidence.go'
              - 'scripts/generate-release-evidence_test.go'
              - 'scripts/release-runtime-contract.json'
              - 'scripts/verify-release-runtime-evidence.py'
              - 'scripts/test_verify_release_runtime_evidence.py'
              - 'app/scripts/rootfs-trusted-sources.json'
              - 'app/scripts/verify-android-linux-runtime.py'
              - 'app/scripts/test_verify_android_linux_runtime.py'
              - 'app/scripts/prepare-android-ubuntu-rootfs.sh'
              - 'app/scripts/test_prepare_android_ubuntu_rootfs.py'

  go-quality:
    name: Go test and vet
    needs: changes
    if: needs.changes.outputs.go == 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 20
    defaults:
      run:
        working-directory: panel/server
    steps:
      - name: Checkout source
        uses: actions/checkout@v5

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}
          cache-dependency-path: panel/server/go.sum

      - name: Run all Go tests
        run: go test ./...

      - name: Run Go vet
        run: go vet ./...

  go-race:
    name: Go race critical packages
    needs: changes
    if: needs.changes.outputs.go == 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 20
    defaults:
      run:
        working-directory: panel/server
    steps:
      - name: Checkout source
        uses: actions/checkout@v5

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}
          cache-dependency-path: panel/server/go.sum

      - name: Run race detector on critical packages
        run: go test -race -count=1 ./router ./database

  route-contract:
    name: Route contract
    needs: changes
    if: needs.changes.outputs.route == 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout source
        uses: actions/checkout@v5

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}
          cache-dependency-path: panel/server/go.sum

      - name: Verify generated route contract
        run: go run ./scripts/generate-route-contract.go -check

  flutter-quality:
    name: Flutter analyze and test
    needs: changes
    if: needs.changes.outputs.flutter == 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 20
    defaults:
      run:
        working-directory: app
    steps:
      - name: Checkout source
        uses: actions/checkout@v5

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: ${{ env.FLUTTER_VERSION }}
          channel: stable
          cache: true

      - name: Resolve Flutter packages
        run: flutter pub get

      - name: Analyze Flutter source
        run: flutter analyze

      - name: Run Flutter tests
        run: flutter test

  kotlin-test:
    name: Kotlin unit tests
    needs: changes
    if: needs.changes.outputs.kotlin == 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout source
        uses: actions/checkout@v5

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}
          cache-dependency-path: panel/server/go.sum

      - name: Set up Node.js for Gradle Web assets
        uses: actions/setup-node@v6
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: npm
          cache-dependency-path: panel/web/package-lock.json

      - name: Enforce Node package manager contract
        run: |
          npm install -g "npm@${NPM_VERSION}"
          node --version | grep -Eq '^v20\.19\.'
          test "$(npm --version)" = "${NPM_VERSION}"

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: ${{ env.JAVA_VERSION }}

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: ${{ env.GRADLE_VERSION }}

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: ${{ env.FLUTTER_VERSION }}
          channel: stable
          cache: true

      - name: Resolve Flutter packages
        working-directory: app
        run: flutter pub get

      - name: Prepare Android Node runtime
        working-directory: app
        run: echo "Android Node runtime is deprecated; Node.js is installed via apk/apt in the rootfs."

      - name: Verify Android Linux runtime rootfs
        working-directory: app
        run: |
          for ABI in $(python3 ../scripts/android-abi-matrix.py list package); do
            python3 scripts/verify-android-linux-runtime.py \
              --rootfs "android/app/src/main/assets/android-runtime/${ABI}/ubuntu/rootfs.tar.gz.bin" \
              --rootfs-sha "android/app/src/main/assets/android-runtime/${ABI}/ubuntu/rootfs.tar.gz.bin.sha256" \
              --rootfs-manifest "android/app/src/main/assets/android-runtime/${ABI}/ubuntu/runtime-manifest.json" \
              --native-dir "android/app/src/main/jniLibs/${ABI}" \
              --native-manifest "android/app/src/main/assets/android-runtime/${ABI}/native-runtime-manifest.json" \
              --trusted-sources scripts/rootfs-trusted-sources.json
          done

      - name: Prepare Android Yaegi runtime
        working-directory: app
        run: ANDROID_RUNTIME_ABIS="$(python3 ../scripts/android-abi-matrix.py list yaegi)" bash scripts/prepare-android-yaegi-runtime.sh

      - name: Run Kotlin tests
        working-directory: app
        run: gradle -p android :app:testReleaseUnitTest --no-daemon --stacktrace

      - name: Test Android Linux runtime verifier
        run: python3 -m unittest scripts/test_android_abi_matrix.py scripts/test_verify_android_update_manifest.py app/scripts/test_sync_android_native_manifest.py app/scripts/test_verify_android_apk_abi.py app/scripts/test_verify_android_linux_runtime.py app/scripts/test_prepare_android_ubuntu_rootfs.py

      - name: Upload Kotlin test reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: kotlin-test-reports
          path: |
            app/build/app/reports/tests/testReleaseUnitTest/
            app/build/app/test-results/testReleaseUnitTest/
          if-no-files-found: warn
          retention-days: 7

  panel-web:
    name: Panel Web build
    needs: changes
    if: needs.changes.outputs.panel_web == 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 15
    defaults:
      run:
        working-directory: panel/web
    steps:
      - name: Checkout source
        uses: actions/checkout@v5

      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: npm
          cache-dependency-path: panel/web/package-lock.json

      - name: Enforce Node package manager contract
        run: |
          npm install -g "npm@${NPM_VERSION}"
          node --version | grep -Eq '^v20\.19\.'
          test "$(npm --version)" = "${NPM_VERSION}"

      - name: Install Panel Web dependencies
        run: npm ci

      - name: Test Panel Web
        run: npm test

      - name: Build Panel Web
        run: npm run build

  release-scripts:
    name: Release and runtime scripts
    needs: changes
    if: needs.changes.outputs.release_scripts == 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout source
        uses: actions/checkout@v5

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}

      - name: Test release and runtime scripts
        run: |
          go test ./scripts/run-runtime-smoke.go ./scripts/run-runtime-smoke_test.go
          go test ./scripts/verify-runtime-contract.go ./scripts/verify-runtime-contract_test.go
          go test ./scripts/verify-runtime-elf.go ./scripts/verify-runtime-elf_test.go
          go test ./scripts/generate-release-evidence.go ./scripts/generate-release-evidence_test.go
          python3 -m unittest scripts/test_version.py scripts/test_android_release_workflow.py scripts/test_android_runtime_smoke.py scripts/test_verify_release_runtime_evidence.py app/scripts/test_verify_android_linux_runtime.py app/scripts/test_prepare_android_ubuntu_rootfs.py
          python3 scripts/version.py check
          go run ./scripts/verify-runtime-contract.go

  quality-gate:
    name: Quality gate
    if: always()
    needs:
      - changes
      - go-quality
      - go-race
      - route-contract
      - flutter-quality
      - kotlin-test
      - panel-web
      - release-scripts
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Require all selected quality jobs to pass
        env:
          JOB_RESULTS: ${{ join(needs.*.result, ' ') }}
        run: |
          if [[ " $JOB_RESULTS " == *" failure "* || " $JOB_RESULTS " == *" cancelled "* ]]; then
            printf 'Quality jobs did not pass: %s\n' "$JOB_RESULTS" >&2
            exit 1
          fi
          printf 'Quality jobs passed or were skipped by path filters: %s\n' "$JOB_RESULTS"
